name: Continuous integration

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  setup:
    name: Setup & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly
          components: rustfmt, clippy

      - name: Ensure cargo in PATH
        run: echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci

      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main

      - name: Setup Protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cargo-make
        run: cargo binstall -y --force cargo-make

      - name: Build
        run: cargo make check

      - name: Upload cargo-make binary
        uses: actions/upload-artifact@v4
        with:
          name: cargo-bin
          path: ~/.cargo/bin/cargo-make
          retention-days: 1

  fmt:
    name: Format check
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly
          components: rustfmt

      - name: Ensure cargo in PATH
        run: echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Download cargo-make binary
        uses: actions/download-artifact@v4
        with:
          name: cargo-bin
          path: ~/.cargo/bin/

      - name: Make cargo-make executable
        run: chmod +x ~/.cargo/bin/cargo-make

      - name: Setup Protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check formatting
        run: cargo make fmt-check

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly
          components: clippy

      - name: Ensure cargo in PATH
        run: echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci

      - name: Setup Protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download cargo-make binary
        uses: actions/download-artifact@v4
        with:
          name: cargo-bin
          path: ~/.cargo/bin/

      - name: Make cargo-make executable
        run: chmod +x ~/.cargo/bin/cargo-make

      - name: Run Clippy linter
        run: cargo make lint

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly

      - name: Ensure cargo in PATH
        run: echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ci

      - name: Setup Protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download cargo-make binary
        uses: actions/download-artifact@v4
        with:
          name: cargo-bin
          path: ~/.cargo/bin/

      - name: Make cargo-make executable
        run: chmod +x ~/.cargo/bin/cargo-make

      - name: Run tests
        run: cargo make test

  deny:
    name: Deny
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run cargo-deny
        uses: EmbarkStudios/cargo-deny-action@v2

  audit:
    name: Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly

      - name: Ensure cargo in PATH
        run: echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main

      - name: Install cargo-audit
        run: cargo binstall -y cargo-audit

      - name: Run cargo-audit
        run: cargo audit
