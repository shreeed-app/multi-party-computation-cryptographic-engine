# HashiCorp Vault configuration.

# Vault HTTP address.
address = ""

# Vault namespace (Vault Enterprise only).
# namespace = ""

# KV v2 mount point, usually "secret" or "kv".
mount = ""

# Base path prefix under the mount.
prefix = ""

# Field in the Vault JSON secret containing the key share (base64-encoded).
field = ""

# Inline token (only for development, not recommended in production).
# token = ""
