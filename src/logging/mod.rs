//! Logging — structured tracing initialization and configuration.
//!
//! `engine` initializes the tracing subscriber and configures log filtering,
//! formatting, and output targets for the runtime.

pub mod engine;
